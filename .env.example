# Where prices come from: yahoo (real, ~15 min delayed) | demo (simulated, no network)
DATA_PROVIDER=yahoo

# Seconds between refreshes. Defaults: 60 for yahoo (its data only changes every ~15 min), 1 for demo.
# POLL_SECONDS=60
